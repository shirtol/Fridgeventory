import styled from "styled-components";
import device from "../../../utils/stylesUtils/mediaQuerySizes";

export const StyledCloseBtn = styled.i`
    position: absolute;
    top: 0;
    right: 0;
    z-index: 200;
    cursor: pointer;
`;
